server:
  http:
    addr: 0.0.0.0:8000
    timeout: 1s
  grpc:
    addr: 0.0.0.0:9000
    timeout: 1s

data:
  database:
    driver: mysql
    # DSN 经环境变量 DATABASE_SOURCE 注入（见 .env / .env.example），不在此文件存放。
    # Kratos config 会把 ${VAR} 占位替换为空，故 NewData 直接读 os.Getenv，不经此字段。
